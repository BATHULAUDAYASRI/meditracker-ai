# Leave unset for same-origin /api (Docker nginx proxies to backend; Vite dev proxies too).
# Only set if you must call a different host, e.g. VITE_API_URL=http://127.0.0.1:5000/api
# VITE_API_URL=
VITE_FIREBASE_API_KEY=
VITE_FIREBASE_AUTH_DOMAIN=
VITE_FIREBASE_PROJECT_ID=
VITE_FIREBASE_APP_ID=
